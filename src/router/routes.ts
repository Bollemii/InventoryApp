/**
 * Type used for the root stack navigator.
 */
export type RootStackParamList = {
    Inventory: undefined;
    Settings: undefined;
    "*": undefined;
};

/**
 * The routes of the application.
 */
export const routes = {
    Inventory: "Inventory",
    Settings: "Settings",
};
